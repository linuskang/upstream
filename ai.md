# AI declaration

AI was used in this project for debugging and fixing purposes only. One of the notable components I used AI for was `/packages/ui/src/components/event.tsx`, as I was figuring out how to position the UIs, and also in generating my `gh/linuskang/i-hate-forms` utility component, which was included in this project.

Other then that, no other AI usage to generate full scripts. Only used for debugging (mostly hydration errors, positioning divs, and generating ci scripts in `.github/workflows`.

No AI usage as present in the making of the backend
